node_modules/
.data/
# .backlog/ — intentionally tracked (project task management)
.worktrees/
.claude/
*.db
*.db-shm
*.db-wal
!server/demo.db
# Policy: do NOT use *.lock — that silently hides package lockfiles (package-lock.json,
# yarn.lock, etc.) which should be committed to pin dependency trees.
# Tuist/.build/ and Tuist/Dependencies/ (below) cover Tuist-generated lock artefacts.

# Electron build output
dist/

# macOS metadata
.DS_Store

# Antigravity CLI sessions
.antigravitycli/

# Internal AI session notes
claude/

# Chrome Web Store submission binaries (use GitHub Releases instead)
chromestore/*.zip
chromestore/*.png

# View screenshots
screenshots/
# VM UI-test screenshots retrieved by run-ui-tests-in-vm.sh (TASK-402)
local-screenshots/

# Xcode / Tuist generated files
*.xcodeproj
*.xcworkspace
DerivedData/
Derived/
build/
Tuist/.build/
Tuist/Dependencies/

# Xcode build output (TASK-344: was accidentally tracked; config moved to config/entitlements/)
build/
*.profraw
