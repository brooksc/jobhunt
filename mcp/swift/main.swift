// JobhuntMCP — stdio→HTTP bridge (DMG only). Implemented in task-059.
// MAS builds exclude this target via the Jobhunt-MAS scheme.
import JobhuntCore

// Entry point placeholder — implementation in task-059.
_ = ModelContainerFactory.self
